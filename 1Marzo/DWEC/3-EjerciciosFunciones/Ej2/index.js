const array = [1,2,3,4,5];

function promedioDet(array, entrada, salida){
    const media = 0;
    for(let i = entrada; i < salida; i++){
        media = array[i];
    }
    return media/(salida-entrada);
}